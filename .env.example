# Development runtime environment variables
# Outlook directory and AI provider settings are collected in onboarding and
# stored in the encrypted local database, not in environment files.

# Log level for the Electron main process.
# Options: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO
